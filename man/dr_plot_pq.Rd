% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr_plot_pq.R
\name{dr_plot_pq}
\alias{dr_plot_pq}
\title{Compare six dimension-reduction techniques on a phyloseq object}
\usage{
dr_plot_pq(
  physeq,
  fact = NULL,
  distance = "bray",
  techniques = c("tsne", "umap", "umato", "pacmap", "mds", "nmds"),
  n_neighbors = 15,
  perplexity = NULL,
  seed = 42
)
}
\arguments{
\item{physeq}{(required) A \link[phyloseq:phyloseq-class]{phyloseq::phyloseq} object with an
\code{otu_table} and \code{sample_data}.}

\item{fact}{(default NULL) Name of a column of \code{sample_data} used to
colour the samples on every panel.}

\item{distance}{(default \code{"bray"}) Dissimilarity index passed to
\code{\link[vegan:vegdist]{vegan::vegdist()}} for t-SNE and to \code{\link[phyloseq:ordinate]{phyloseq::ordinate()}} for MDS and
NMDS. Not used by the count-matrix techniques (UMAP, UMATO, PaCMAP).}

\item{techniques}{(default \code{c("tsne", "umap", "umato", "pacmap", "mds", "nmds")}) Which panels to draw. Whatever the order of the vector, the
panels are always arranged in three fixed family columns — \strong{Local}
(UMAP, t-SNE), \strong{Global} (MDS, NMDS) and \strong{Local/Global trade-off}
(UMATO, PaCMAP) — each announced by a column header, so a missing
technique leaves an empty cell in its family column. Use a subset to
skip techniques whose backend is unavailable (e.g. the Python ones) or
to speed up a quick look.}

\item{n_neighbors}{(int, default 15) Number of neighbours for the
count-matrix techniques (UMAP, UMATO, PaCMAP). Capped internally when
the number of samples is small.}

\item{perplexity}{(numeric, default NULL) Perplexity of t-SNE. With the
default \code{NULL}, it is set to \code{min(30, floor((nsamples - 1) / 3))} so
that small datasets run.}

\item{seed}{(default 42) Seed used for reproducibility: \code{\link[=set.seed]{set.seed()}}
before the R computations, and \code{random_state} for the Python backends.}
}
\value{
A patchwork ggplot. Every panel is titled by the technique name and
subtitled by its own backend parameters (\code{perplexity} for t-SNE,
\code{n_neighbors} — and \code{hub_num} — for the count-matrix techniques,
\code{stress} for NMDS, explained variance for MDS); a single colour legend
for \code{fact}, shared by all panels, sits below the grid. The embedding
data frames (one per technique, each with the two coordinate columns,
\code{Sample} and every \code{sample_data} column) are attached as a named list
in the attribute \code{layouts}, so they can be reused without recomputing.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Apply up to six dimensionality-reduction techniques to the samples of a
phyloseq object and draw them side by side in a single ggplot (patchwork):
t-SNE, UMAP, UMATO and PaCMAP (local / compromise techniques) plus MDS and
NMDS (global techniques). Because each technique preserves a different
aspect of the data, comparing them on one figure is the safest way to read
an embedding (Jeon et al., 2026; Wang et al., 2021).

The t-SNE, UMATO and PaCMAP panels come from \code{ggplotpq} (\code{\link[MiscMetabar:tsne_pq]{tsne_pq()}} is a
re-export of \code{\link[MiscMetabar:tsne_pq]{MiscMetabar::tsne_pq()}}; \code{\link[=umato_pq]{umato_pq()}} and \code{\link[=pacmap_pq]{pacmap_pq()}} call
Python through \code{reticulate}); the UMAP panel uses \code{\link[MiscMetabar:umap_pq]{MiscMetabar::umap_pq()}};
the MDS and NMDS panels use \code{\link[phyloseq:ordinate]{phyloseq::ordinate()}} on the distance given
by \code{distance}.
}
\details{
No transformation is done inside the function: rarefy or transform
\code{physeq} beforehand if needed (e.g. \code{\link[phyloseq:rarefy_even_depth]{phyloseq::rarefy_even_depth()}},
\code{\link[phyloseq:transform_sample_counts]{phyloseq::transform_sample_counts()}}).

t-SNE, UMAP and friends are \strong{local} techniques: they are reliable for
neighbourhood, outlier and cluster identification, but the distances
between points or clusters, the cluster density and the class separability
read on the embedding must not be interpreted as faithful to the original
space. MDS (PCoA) and NMDS are \strong{global} techniques whose point positions
better reflect the supplied distance matrix. UMATO (Jeon et al., 2025) and
PaCMAP (Wang et al., 2021) seek a compromise between the two worlds —
which is precisely why they are worth comparing with the six-panel grid.

The Python packages \code{reticulate} (R side) and \code{umato} / \code{pacmap}
(Python side) are required for the UMATO and PaCMAP panels; install the
Python modules with
\code{reticulate::py_install(c("umato", "pacmap"), pip = TRUE)}.
}
\examples{
data(data_fungi_mini, package = "MiscMetabar")
\donttest{
if (requireNamespace("Rtsne", quietly = TRUE)) {
  p <- dr_plot_pq(
    prune_samples(sample_names(data_fungi_mini)[1:20], data_fungi_mini),
    fact = "Height",
    techniques = c("tsne", "mds", "nmds")
  )
  p
}
}
\dontrun{
dr_plot_pq(data_fungi_mini, fact = "Height")
}
}
\references{
Jeon, H., Park, J., Shin, S., & Seo, J. (2026). Stop Misusing t-SNE and UMAP
for Visual Analytics. \emph{IEEE Transactions on Visualization and Computer
Graphics}. \doi{10.48550/arXiv.2506.08725}

Jeon, H., Ko, K., Lee, S., Hyun, J., Yang, T., Go, G., Jo, J., & Seo, J.
(2025). UMATO: Bridging Local and Global Structures for Reliable Visual
Analytics with Dimensionality Reduction. \emph{IEEE Transactions on
Visualization and Computer Graphics}. \doi{10.1109/TVCG.2025.3602735}

Wang, Y., Huang, H., Rudin, C., & Shaposhnik, Y. (2021). Understanding How
Dimension Reduction Tools Work: An Empirical Approach to Deciphering
t-SNE, UMAP, TriMap, and PaCMAP for Data Visualization. \emph{Journal of
Machine Learning Research}, 22(201), 1-73.
\doi{10.48550/arXiv.2012.04456}
}
\seealso{
\code{\link[=umato_pq]{umato_pq()}}, \code{\link[=pacmap_pq]{pacmap_pq()}}, \code{\link[MiscMetabar:umap_pq]{MiscMetabar::umap_pq()}},
\code{\link[MiscMetabar:tsne_pq]{MiscMetabar::tsne_pq()}}, \code{\link[MiscMetabar:plot_ordination_pq]{MiscMetabar::plot_ordination_pq()}}
}
\author{
Adrien Taudière
}
