% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pacmap_pq.R
\name{pacmap_pq}
\alias{pacmap_pq}
\title{PaCMAP embedding of a phyloseq object (Python backend)}
\usage{
pacmap_pq(physeq, n_neighbors = 10, ...)
}
\arguments{
\item{physeq}{(required) A \link[phyloseq:phyloseq-class]{phyloseq::phyloseq} object with an
\code{otu_table} and \code{sample_data}. Samples (rows of the count matrix) are
embedded; taxa are features.}

\item{n_neighbors}{(int, default 10) Number of neighbour pairs. PaCMAP
itself reorganises the pair budget when the sample size is too small
(see its warnings). Passed to \code{pacmap.PaCMAP()}.}

\item{...}{Additional arguments passed to \code{pacmap.PaCMAP()}
(e.g. \code{distance}, \code{MN_ratio}, \code{FP_ratio}, \code{num_iters}, \code{apply_pca},
\code{random_state}).}
}
\value{
A tibble with the columns \code{x_pacmap}, \code{y_pacmap}, \code{Sample}
(sample name) followed by every column of \code{sample_data}.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Compute a 2-dimensional PaCMAP embedding (Wang et al., 2021) of the samples
of a phyloseq object. PaCMAP optimises the preservation of both local and
global structure by balancing three kinds of pair constraints
(neighbour pairs, mid-near pairs and far pairs), and was among the best
compromises in the empirical comparison of Wang et al. (2021). The
embedding is computed by the Python package \code{pacmap} through
\code{\link[reticulate:import]{reticulate::import()}}.
}
\details{
No transformation is done inside the function: the raw count matrix
(samples × taxa) is handed to the backend as is (with \code{apply_pca = TRUE},
the PaCMAP default, an initial PCA projection is computed internally).
Transform \code{physeq} beforehand if needed
(e.g. \code{\link[phyloseq:transform_sample_counts]{phyloseq::transform_sample_counts()}}).

PaCMAP improves the \emph{global} readability of the embedding, but distances
between clusters and cluster densities must still be interpreted with
care. For questions about point or cluster distances in the original
space, prefer a global technique: PCA (\code{\link[stats:prcomp]{stats::prcomp()}}) or MDS/PCoA
(\code{\link[phyloseq:ordinate]{phyloseq::ordinate()}}, \code{\link[MiscMetabar:plot_ordination_pq]{MiscMetabar::plot_ordination_pq()}}).
\code{\link[=dr_plot_pq]{dr_plot_pq()}} draws PaCMAP next to t-SNE, UMAP, UMATO, MDS and NMDS for
a side-by-side comparison.

The Python packages \code{reticulate} (R side) and \code{pacmap} (Python side) are
required; an informative error is thrown when either is missing
(install the Python module with
\code{reticulate::py_install("pacmap", pip = TRUE)}).
}
\examples{
\dontrun{
data(data_fungi_mini, package = "MiscMetabar")
df_pacmap <- pacmap_pq(data_fungi_mini, n_neighbors = 10, random_state = 42)
ggplot2::ggplot(
  df_pacmap,
  ggplot2::aes(x = x_pacmap, y = y_pacmap, col = Height)
) +
  ggplot2::geom_point(size = 2)
}
}
\references{
Wang, Y., Huang, H., Rudin, C., & Shaposhnik, Y. (2021). Understanding How
Dimension Reduction Tools Work: An Empirical Approach to Deciphering
t-SNE, UMAP, TriMap, and PaCMAP for Data Visualization. \emph{Journal of
Machine Learning Research}, 22(201), 1-73.
\doi{10.48550/arXiv.2012.04456}

Jeon, H., Park, J., Shin, S., & Seo, J. (2026). Stop Misusing t-SNE and UMAP
for Visual Analytics. \emph{IEEE Transactions on Visualization and Computer
Graphics}. \doi{10.48550/arXiv.2506.08725}
}
\seealso{
\code{\link[=umato_pq]{umato_pq()}}, \code{\link[=dr_plot_pq]{dr_plot_pq()}}, \code{\link[MiscMetabar:umap_pq]{MiscMetabar::umap_pq()}},
\code{\link[MiscMetabar:tsne_pq]{MiscMetabar::tsne_pq()}}
}
\author{
Adrien Taudière
}
