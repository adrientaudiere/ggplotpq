% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/membership_from_list.R
\name{membership_from_list}
\alias{membership_from_list}
\title{Convert a named list of members into a binary membership data frame}
\usage{
membership_from_list(x, keep_rownames = TRUE)
}
\arguments{
\item{x}{(named list, required) Each element is a vector of members
belonging to one set. The list names become the column names of the
returned data frame. At least two sets are required.}

\item{keep_rownames}{(logical, default \code{TRUE}) If \code{TRUE}, the unique
members are stored as the row names of the returned data frame.}
}
\value{
A \link{data.frame} with one column per set (named after the list
elements) and one row per unique non-\code{NA} member across all sets.
Cell values are \code{TRUE}/\code{FALSE}. When \code{keep_rownames = TRUE} the row
names identify the members.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Internal helper that turns a named list of character (or other) vectors
— each vector listing the members belonging to one set — into a
wide-format data frame with one row per unique member and one logical
column per set. This is the format expected by
\code{\link[ComplexUpset:upset]{ComplexUpset::upset()}} and similar venn/upset plotting tools, which
cannot consume a list directly.

Conceptually equivalent to \code{\link[UpSetR:fromList]{UpSetR::fromList()}} but kept inside the
package to avoid a hard dependency on \pkg{UpSetR} and to return
logical columns (which both \pkg{ComplexUpset} and \pkg{ggplot2}
handle natively).
}
\examples{
\dontrun{
membership <- list(
  bact  = c("s1", "s2", "s3"),
  fungi = c("s1", "s3"),
  amf   = c("s2")
)
ggplotpq:::membership_from_list(membership)
#        bact fungi   amf
# s1    TRUE  TRUE FALSE
# s2    TRUE FALSE  TRUE
# s3    TRUE  TRUE FALSE
}
}
\author{
Adrien Taudière
}
\keyword{internal}
