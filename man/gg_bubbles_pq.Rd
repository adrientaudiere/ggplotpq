% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_bubbles_pq.R
\name{gg_bubbles_pq}
\alias{gg_bubbles_pq}
\title{Circle-packed bubble plot of a phyloseq object using ggplot2}
\usage{
gg_bubbles_pq(
  physeq,
  rank_label = "Taxa",
  rank_color = "Family",
  rank_contour = NULL,
  layout = c("circle", "square"),
  facet_by = NULL,
  log1ptransform = FALSE,
  min_nb_seq = 0,
  label_size = 2,
  label_color = "grey10",
  show_labels = TRUE,
  border_color = "white",
  border_width = 0.5,
  alpha = 0.8,
  npoints = 50,
  ncol_facet = NULL,
  return_dataframe = FALSE,
  diff_contour = FALSE,
  diff_contour_colors = c("#E41A1C", "#377EB8", "#4DAF4A", "#984EA3"),
  diff_border_width = 1.5,
  show_title = TRUE
)
}
\arguments{
\item{physeq}{(required) A \link[phyloseq:phyloseq-class]{phyloseq::phyloseq} object.}

\item{rank_label}{(character, default \code{"Taxa"}) Column in \verb{@tax_table} to
label circles. When \code{"Taxa"}, taxa names are used.}

\item{rank_color}{(character, default \code{"Family"}) Column in \verb{@tax_table} to
colour circles.}

\item{rank_contour}{(character, default \code{NULL}) Column in \verb{@tax_table} to
colour circle borders. When \code{NULL}, \code{border_color} is used uniformly.
Ignored when \code{diff_contour = TRUE}.}

\item{layout}{(character, default \code{"circle"}) Packing layout: \code{"circle"}
for tight circular packing, \code{"square"} for a square boundary.}

\item{facet_by}{(character, default \code{NULL}) Column name from \verb{@sam_data} to
facet the plot. One bubble chart is produced per level.}

\item{log1ptransform}{(logical, default \code{FALSE}) If \code{TRUE}, sequence counts
are log1p-transformed before computing circle sizes.}

\item{min_nb_seq}{(integer, default \code{0}) Minimum sequence count; taxa below
this threshold are dropped.}

\item{label_size}{(numeric, default \code{2}) Font size for labels inside circles.}

\item{label_color}{(character, default \code{"grey10"}) Colour for label text.}

\item{show_labels}{(logical, default \code{TRUE}) If \code{TRUE}, labels are shown
inside circles that are large enough to fit text.}

\item{border_color}{(character, default \code{"white"}) Colour for circle borders
(used when \code{rank_contour = NULL}).}

\item{border_width}{(numeric, default \code{0.5}) Width of circle borders.}

\item{alpha}{(numeric, default \code{0.8}) Transparency of circle fill.}

\item{npoints}{(integer, default \code{50}) Vertices per circle polygon. Higher
values produce smoother circles.}

\item{ncol_facet}{(integer, default \code{NULL}) Number of columns for
\code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}. Ignored when \code{diff_contour = TRUE}.}

\item{return_dataframe}{(logical, default \code{FALSE}) If \code{TRUE}, return the
data frame used for plotting instead of a ggplot object. Ignored when
\code{diff_contour = TRUE}.}

\item{diff_contour}{(logical, default \code{FALSE}) If \code{TRUE} and \code{facet_by} is
set, produces pairwise comparison panels for all facet-level pairs using
\pkg{patchwork}. Taxa unique to each side are highlighted with a distinct
contour colour; shared taxa receive a transparent contour.}

\item{diff_contour_colors}{(character vector, default
\code{c("#E41A1C", "#377EB8", "#4DAF4A", "#984EA3")}) Border colours for taxa
unique to each facet level. Recycled if shorter than the number of levels.}

\item{diff_border_width}{(numeric, default \code{1.5}) Border width in
\code{diff_contour} mode.}

\item{show_title}{(logical, default \code{TRUE}) If \code{TRUE}, adds an informative
title describing the fill, contour, size, and label mappings.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object, a patchwork object (when
\code{diff_contour = TRUE}), or a data.frame (when \code{return_dataframe = TRUE}).
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Creates a static circle-packed bubble plot of taxa abundances from a
phyloseq object using ggplot2. Circles can be packed in a circular layout
(tight, default) or a square layout. Optionally facets the plot by a sample
data variable, producing one bubble chart per level.

When \code{diff_contour = TRUE} together with \code{facet_by}, all pairwise
comparisons between facet levels are shown side by side using
\pkg{patchwork}. For each pair (A vs B), taxa unique to A are highlighted
with A's colour and taxa unique to B with B's colour. Shared taxa receive
a transparent contour, making it easy to spot which taxa are exclusive to
each group.

Migrated from \code{comparpq::gg_bubbles_pq()} (single-phyloseq variant —
list_phyloseq support was removed; use \code{comparpq::gg_bubbles_pq()} for
multi-object comparisons).
}
\examples{
\donttest{
data(data_fungi_mini, package = "MiscMetabar")
gg_bubbles_pq(physeq = data_fungi_mini, rank_color = "Class")

gg_bubbles_pq(
  physeq = data_fungi_mini, rank_color = "Class",
  rank_contour = "Order"
)

gg_bubbles_pq(
  physeq = data_fungi_mini, rank_color = "Class",
  layout = "square"
)
}

\dontrun{
# Faceted by sample variable
data(data_fungi, package = "MiscMetabar")
gg_bubbles_pq(
  physeq = data_fungi, rank_color = "Order",
  facet_by = "Height", min_nb_seq = 100
)

# Pairwise diff_contour
gg_bubbles_pq(
  physeq = data_fungi, rank_color = "Order",
  facet_by = "Height", min_nb_seq = 100,
  diff_contour = TRUE, show_labels = FALSE
)
}
}
\seealso{
\code{\link[comparpq:gg_bubbles_pq]{comparpq::gg_bubbles_pq()}} for the multi-phyloseq variant.
}
\author{
Adrien Taudière
}
