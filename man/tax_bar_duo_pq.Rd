% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tax_bar_duo_pq.R
\name{tax_bar_duo_pq}
\alias{tax_bar_duo_pq}
\title{Side-by-side sequence and taxa composition bar plots}
\usage{
tax_bar_duo_pq(
  physeq,
  fact = "Sample",
  taxa = "Order",
  title = paste0(taxa, " composition"),
  subtitle = c("Number of sequences (\%)", "Number of taxa"),
  show_values = c(TRUE, TRUE),
  minimum_value_to_show = c(0.05, 4),
  reorder_colors = TRUE,
  alternate_lightness = TRUE,
  add_ribbon = TRUE,
  legend_position = "bottom",
  legend_nrow = NULL,
  title_size = 20,
  subtitle_size = 11,
  ...
)
}
\arguments{
\item{physeq}{(required) A \link[phyloseq:phyloseq-class]{phyloseq::phyloseq} object.}

\item{fact}{(default: "Sample") Name of the column in the \code{sam_data} slot
used to group samples on the x-axis.}

\item{taxa}{(default: "Order") Name of the taxonomic rank used to fill the
bars.}

\item{title}{(default: \code{paste0(taxa, " composition")}) Overall title added
with \code{\link[patchwork:plot_annotation]{patchwork::plot_annotation()}}. Use \code{NULL} for no title.}

\item{subtitle}{(default: \code{c("Number of sequences (\%)", "Number of taxa")})
Subtitle of the left and right panels. A length one value is used for
both panels. Use \code{NULL} for no subtitle.}

\item{show_values}{(logical, default \code{c(TRUE, TRUE)}) Passed to
\code{\link[MiscMetabar:tax_bar_pq]{MiscMetabar::tax_bar_pq()}}. The first value applies to the left
(sequence) panel, the second to the right (taxa) panel. A length one
value is recycled.}

\item{minimum_value_to_show}{(default: \code{c(0.05, 4)}) Passed to
\code{\link[MiscMetabar:tax_bar_pq]{MiscMetabar::tax_bar_pq()}}. As for \code{show_values}, the first value
applies to the left (sequence, in percent) panel and the second to the
right (taxa, in counts) panel.}

\item{reorder_colors}{(logical, default TRUE) If TRUE,
\code{\link[=reorder_distinct_colors]{reorder_distinct_colors()}} is applied to both panels to maximize the
perceptual contrast between adjacent bar segments.}

\item{alternate_lightness}{(logical, default TRUE) Passed to
\code{\link[=reorder_distinct_colors]{reorder_distinct_colors()}}. Only used when \code{reorder_colors = TRUE}.}

\item{add_ribbon}{(logical, default TRUE) Passed to
\code{\link[MiscMetabar:tax_bar_pq]{MiscMetabar::tax_bar_pq()}}: draw ribbons connecting the bars of
consecutive modalities.}

\item{legend_position}{(default: "bottom") Position of the single
collected legend, passed to the \code{legend.position} theme element. Use
"right" to get the usual side legend, or "none" to drop the legend.}

\item{legend_nrow}{(default: NULL) Number of rows of the collected legend.
Only relevant when \code{legend_position} is "bottom" or "top", where the
default single row quickly becomes unreadable for a taxonomic rank with
many levels.}

\item{title_size}{(default: 20) Font size of the overall title.}

\item{subtitle_size}{(default: 11) Font size of the panel subtitles.}

\item{...}{Other arguments passed on to \code{\link[MiscMetabar:tax_bar_pq]{MiscMetabar::tax_bar_pq()}} for
\strong{both} panels (e.g. \code{order_modality}, \code{label_taxa}, \code{bar_width}).}
}
\value{
A \code{patchwork} object.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Build the two complementary \code{\link[MiscMetabar:tax_bar_pq]{MiscMetabar::tax_bar_pq()}} views of the same
\code{physeq} object in a single call and assemble them with \code{patchwork}:
\itemize{
\item \strong{left panel}: relative number of sequences
(\code{percent_bar = TRUE}, \code{nb_seq = TRUE}),
\item \strong{right panel}: number of taxa
(\code{percent_bar = FALSE}, \code{nb_seq = FALSE}).
}

The fill guide is collected into a single shared legend, and one overall
title is added above the two panels through
\code{\link[patchwork:plot_annotation]{patchwork::plot_annotation()}}.
}
\note{
Both panels are computed from the same \code{physeq} object and the same
\code{taxa} rank, so their discrete fill scales share the exact same set of
levels. \code{\link[=reorder_distinct_colors]{reorder_distinct_colors()}} is deterministic for a given set of
levels: each taxon therefore receives the same color in both panels, and
\code{patchwork} can merge the two guides into one legend.
}
\examples{
\donttest{
if (requireNamespace("patchwork", quietly = TRUE)) {
  data(data_fungi_mini, package = "MiscMetabar")
  tax_bar_duo_pq(data_fungi_mini, "Height", taxa = "Genus")
  tax_bar_duo_pq(data_fungi_mini, "Height",
    taxa = "Genus",
    title = "Composition en genres",
    subtitle = c("Nombre de séquences en \%", "Nombre de taxons"),
    minimum_value_to_show = c(0.05, 4)
  )
}
}
}
\seealso{
\code{\link[MiscMetabar:tax_bar_pq]{MiscMetabar::tax_bar_pq()}}, \code{\link[=reorder_distinct_colors]{reorder_distinct_colors()}}
}
\author{
Adrien Taudière
}
