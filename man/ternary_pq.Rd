% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ternary_pq.R
\name{ternary_pq}
\alias{ternary_pq}
\title{Ternary and diamond plots for 3-or-4-group taxon compositions}
\usage{
ternary_pq(
  physeq,
  group,
  level_order = NULL,
  raw = FALSE,
  normalize_groups = TRUE,
  color_by = NULL,
  size_by = c("abundance", "log2_abundance", "equal"),
  label_by = NULL,
  label_size = 2.5,
  add_grid = TRUE,
  point_alpha = 0.8
)
}
\arguments{
\item{physeq}{(required) A \link[phyloseq:phyloseq-class]{phyloseq::phyloseq} object.}

\item{group}{(required) Either a single character string matching a
variable name in \code{sample_data(physeq)}, or a factor of length
\code{nsamples(physeq)}. Must have exactly 3 (ternary) or 4 (diamond) levels.}

\item{level_order}{(character vector, default \code{NULL}) Order of levels; for
ternary plots this is (left, right, top); for diamond plots (right, top,
left, bottom). When \code{NULL}, the natural factor level order is used.}

\item{raw}{(logical, default \code{FALSE}) If \code{TRUE}, raw counts are used
instead of relative abundances.}

\item{normalize_groups}{(logical, default \code{TRUE}) If \code{TRUE} (and \code{raw = FALSE}), each group is given equal weight regardless of its sample count.
If \code{FALSE}, weight is proportional to sample count.}

\item{color_by}{(character, default \code{NULL}) Column name from \verb{@tax_table}
to map to point colour.}

\item{size_by}{(character, default \code{"abundance"}) How to scale point size.
One of \code{"abundance"} (overall relative abundance), \code{"log2_abundance"}, or
\code{"equal"} (all the same size).}

\item{label_by}{(character, default \code{NULL}) Column name from \verb{@tax_table}
(or \code{"taxon"} for row names) to annotate points with text labels.}

\item{label_size}{(numeric, default \code{2.5}) Text size for labels.}

\item{add_grid}{(logical, default \code{TRUE}) If \code{TRUE}, a 10-interval
reference grid is drawn inside the triangle / diamond.}

\item{point_alpha}{(numeric, default \code{0.8}) Transparency of the points.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Displays each taxon as a point inside a triangle (3 groups) or a diamond
(4 groups) whose corners represent the sample groups. A taxon plotted
near one corner is most abundant in that group.

The coordinate math follows the standard de Finetti diagram
(Wikipedia: Ternary plot):
\deqn{x = \frac{2b + c}{2(a+b+c)}, \quad y = \frac{\sqrt{3}\,c}{2(a+b+c)}}
where \eqn{a,b,c} are the mean relative abundances in the left, right, and
top groups respectively.

The diamond projection for 4 groups uses:
\deqn{x = \frac{a - c}{a+b+c+d}, \quad y = \frac{b - d}{a+b+c+d}}
(right, top, left, bottom).

This is an independent implementation; the coordinate formulas are in the
public domain (de Finetti 1926; see also Armstrong 2014, IEEE VIS).
}
\examples{
\donttest{
data(data_fungi_mini, package = "MiscMetabar")
# Subset to samples with non-NA Height (3 levels: Low, High + one more)
pq3 <- phyloseq::subset_samples(
  data_fungi_mini,
  !is.na(Height) & Height \%in\% c("Low", "High")
)
# Need 3-level group; use Time as example (if it has 3 levels)
time_lvls <- unique(na.omit(
  as.character(phyloseq::sample_data(data_fungi_mini)[["Time"]])
))
if (length(time_lvls) == 3) {
  pq_t <- phyloseq::subset_samples(
    data_fungi_mini,
    !is.na(Time)
  )
  ternary_pq(pq_t, group = "Time")
}
}

\dontrun{
ternary_pq(pq_t, group = "Time", color_by = "Family", add_grid = FALSE)
ternary_pq(pq_t, group = "Time", size_by = "log2_abundance")
}
}
\author{
Adrien Taudière
}
