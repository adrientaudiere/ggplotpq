% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/community_sharing_plot.R
\name{community_sharing_plot}
\alias{community_sharing_plot}
\title{Community sharing plot: modalities as pie nodes with multi-metric links}
\usage{
community_sharing_plot(
  physeq,
  fact,
  metrics = default_sharing_metrics(),
  pie_taxrank = "Class",
  pie_r = 0.28,
  label_offset = 0.18,
  curvature_range = c(-0.35, 0.35),
  linewidth_range = c(0.8, 4.5),
  n_perm = 0,
  sig_threshold = 0.05,
  nonsig_alpha = 0.15,
  seed = NULL,
  palette = "Set3",
  max_taxa = 12,
  other_color = "grey70",
  show_na_modality = FALSE,
  show_na = TRUE,
  na_color = "grey40",
  pie_border_color = "black",
  pie_border_width = 0.6,
  title = NULL,
  base_size = 12
)
}
\arguments{
\item{physeq}{(required) A \link[phyloseq:phyloseq-class]{phyloseq::phyloseq} object.}

\item{fact}{(required, character) Name of a \code{sample_data(physeq)} column used
to group samples into modalities. Must have 2 to 4 unique values.}

\item{metrics}{(named list, default \code{\link[=default_sharing_metrics]{default_sharing_metrics()}}) Metric
definitions. See \code{\link[=default_sharing_metrics]{default_sharing_metrics()}} and \code{\link[=make_sharing_metric]{make_sharing_metric()}}.}

\item{pie_taxrank}{(character, default \code{"Class"}) Taxonomic rank for the pie
charts.}

\item{pie_r}{(numeric, default \code{0.28}) Pie radius in data units.}

\item{label_offset}{(numeric, default \code{0.18}) Distance between pie edge and
node label.}

\item{curvature_range}{(numeric of length 2, default \code{c(-0.35, 0.35)}) Range
of \code{\link[ggplot2:geom_curve]{ggplot2::geom_curve()}} curvatures used to fan out metrics between a
pair of nodes. The sign is flipped per pair so that the first metric always
fans toward the plot centre (and the last toward the border).}

\item{linewidth_range}{(numeric of length 2, default \code{c(0.8, 4.5)})
\code{linewidth_range[1]} corresponds to the weakest similarity,
\code{linewidth_range[2]} to the strongest. For bounded metrics
(\code{bounds = c(0, 1)}), the mapping is global across runs; for unbounded
metrics it is scaled within the observed range.}

\item{n_perm}{(integer, default \code{0}) Number of label-permutation iterations
for significance testing. \code{0} disables the test. See Details.}

\item{sig_threshold}{(numeric in \verb{(0, 1)}, default \code{0.05}) p-value threshold
below which a link is considered significant. Only used when \code{n_perm > 0}.}

\item{nonsig_alpha}{(numeric in \verb{[0, 1]}, default \code{0.15}) Alpha applied to
non-significant links. Significant links keep alpha \code{0.75}. Only used when
\code{n_perm > 0}.}

\item{seed}{(integer or \code{NULL}, default \code{NULL}) Passed to \code{\link[=set.seed]{set.seed()}} before
the permutation loop for reproducibility.}

\item{palette}{(character, default \code{"Set3"}) Either the name of an
RColorBrewer qualitative palette or a character vector of fill colours for
the top taxa.}

\item{max_taxa}{(integer, default \code{12}) Keep colours for the \code{max_taxa} most
abundant taxa (summed across modalities) and collapse the rest into an
\code{"Other"} category filled with \code{other_color}.}

\item{other_color}{(character, default \code{"grey70"}) Fill colour for \code{"Other"}.}

\item{show_na_modality}{(logical, default \code{FALSE}) If \code{TRUE}, samples whose
\code{fact} value is \code{NA} are grouped into an extra \code{"NA"} modality. The total
number of modalities (including \code{"NA"}) must still be between 2 and 4.}

\item{show_na}{(logical, default \code{TRUE}) If \code{TRUE}, taxa with an \code{NA}/empty
value at \code{pie_taxrank} are shown as a dedicated \code{"NA"} category filled with
\code{na_color}. If \code{FALSE}, they are dropped.}

\item{na_color}{(character, default \code{"grey40"}) Fill colour for the \code{"NA"}
taxonomic category.}

\item{pie_border_color}{(character, default \code{"black"}) Colour of the circle
drawn around each pie.}

\item{pie_border_width}{(numeric, default \code{0.6}) Linewidth of the pie border.}

\item{title}{(character, default \code{NULL}) Plot title.}

\item{base_size}{(numeric, default \code{12}) Base font size.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Draws a figure with one node per modality of \code{fact} (2 to 4 supported),
positioned on a regular polygon. Each node is a pie chart showing the
taxonomic composition at rank \code{pie_taxrank}. Between each pair of nodes, one
curved link per metric is drawn; linewidth is rescaled within each metric to
\code{linewidth_range}. A legend below the figure gives each metric's min / mean /
max over all pairs.
}
\details{
\strong{Permutation null model.} When \code{n_perm > 0}, significance is assessed by
label permutation: the \code{fact} column in \code{sample_data(physeq)} is shuffled
uniformly at random among samples (preserving group sizes), then all \code{prep()}
functions and metric computations are re-run on the permuted data. This is
repeated \code{n_perm} times. The empirical p-value for each pair x metric
combination is the proportion of permuted values greater than or equal to the
observed value (one-sided upper-tail test). Non-significant links are drawn
with alpha \code{nonsig_alpha} (faded); significant links use alpha \code{0.75}.

\strong{Performance.} Each permutation re-runs all \code{prep()} functions. For metrics
that call \code{\link[phyloseq:tax_glom]{phyloseq::tax_glom()}} (e.g. \code{genus_prop}), this can be slow on
large datasets. Recommended range: \code{n_perm = 99} to \code{n_perm = 199}.
}
\examples{
\donttest{
data(data_fungi_mini, package = "MiscMetabar")
pkgs <- c("ggforce", "purrr", "tidyr", "scales", "RColorBrewer", "vegan")
if (all(vapply(pkgs, requireNamespace, logical(1), quietly = TRUE))) {
  # Default: 4 metrics, pie charts at Class rank, Height has 3 modalities
  community_sharing_plot(data_fungi_mini, fact = "Height")
}
}
\dontrun{
# Only show a single metric (Jaccard)
community_sharing_plot(
  data_fungi_mini,
  fact    = "Height",
  metrics = default_sharing_metrics()["jac_sim"]
)

# Use Phylum rank for the pie charts and a different palette
community_sharing_plot(
  data_fungi_mini,
  fact        = "Height",
  pie_taxrank = "Phylum",
  palette     = "Set2",
  max_taxa    = 8
)

# Include samples with NA height as a fourth modality
community_sharing_plot(data_fungi_mini, fact = "Height", show_na_modality = TRUE)

# Permutation significance test (99 permutations, faded non-sig links)
community_sharing_plot(data_fungi_mini, fact = "Height", n_perm = 99, seed = 42)
}
}
\seealso{
\code{\link[=community_sharing_barplot]{community_sharing_barplot()}}, \code{\link[=make_sharing_metric]{make_sharing_metric()}},
\code{\link[=default_sharing_metrics]{default_sharing_metrics()}}
}
\author{
Adrien Taudière
}
