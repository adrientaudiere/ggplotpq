% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/community_sharing_plot.R
\name{community_sharing_barplot}
\alias{community_sharing_barplot}
\title{Companion bar chart for \code{community_sharing_plot()}}
\usage{
community_sharing_barplot(
  physeq,
  fact,
  metrics = default_sharing_metrics(),
  show_na_modality = FALSE,
  base_size = 12,
  title = NULL
)
}
\arguments{
\item{physeq}{(required) A \link[phyloseq:phyloseq-class]{phyloseq::phyloseq} object.}

\item{fact}{(required, character) Name of a \code{sample_data(physeq)} column.
Must have 2 to 4 unique values.}

\item{metrics}{(named list, default \code{\link[=default_sharing_metrics]{default_sharing_metrics()}}) Metric
definitions from \code{\link[=make_sharing_metric]{make_sharing_metric()}}.}

\item{show_na_modality}{(logical, default \code{FALSE}) Same meaning as in
\code{\link[=community_sharing_plot]{community_sharing_plot()}}.}

\item{base_size}{(numeric, default \code{12}) Base font size.}

\item{title}{(character, default \code{NULL}) Plot title.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Computes the same pairwise metrics as \code{\link[=community_sharing_plot]{community_sharing_plot()}} and displays
them as grouped bars, with \strong{one panel per metric} and a free y-axis. Each
metric is therefore compared across pairs on its own scale, which matters
because metrics live on incomparable scales (e.g. a shared-species count
dwarfs a Bray-Curtis similarity in \verb{[0, 1]}). Useful for precise numerical
comparison alongside the network figure.
}
\examples{
\donttest{
data(data_fungi_mini, package = "MiscMetabar")
if (all(vapply(c("purrr", "tidyr", "vegan"), requireNamespace,
  logical(1), quietly = TRUE))) {
  # One panel per metric, pairs on the x-axis, free y-scale per metric
  community_sharing_barplot(data_fungi_mini, fact = "Height")
}
}
}
\seealso{
\code{\link[=community_sharing_plot]{community_sharing_plot()}}
}
\author{
Adrien Taudière
}
