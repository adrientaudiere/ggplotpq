% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_taxa_heatmap_pq.R
\name{plot_taxa_heatmap_pq}
\alias{plot_taxa_heatmap_pq}
\title{Heatmap of the top-N most abundant taxa across samples}
\usage{
plot_taxa_heatmap_pq(
  physeq,
  n_top = 20,
  taxa_rank = "Family",
  log10 = FALSE,
  na_rm = TRUE,
  fill_scale = NULL
)
}
\arguments{
\item{physeq}{(required) A \link[phyloseq:phyloseq-class]{phyloseq::phyloseq} object.}

\item{n_top}{(integer, default 20) Number of top taxa (by total read
count across samples) to display. Must be >= 1.}

\item{taxa_rank}{(character, default "Family") The taxonomic rank
used to label the y-axis. Must be a column in
\code{\link[phyloseq:tax_table]{phyloseq::tax_table()}}.}

\item{log10}{(logical, default FALSE) If TRUE, the cell value is
log10(\code{Abundance + 1}) — useful when the abundance distribution is
heavily right-skewed.}

\item{na_rm}{(logical, default TRUE) Drop samples with NA in the
abundance before plotting.}

\item{fill_scale}{(function, default NULL) Optional ggplot2 fill
scale applied to the heatmap. If NULL, uses
\code{\link[ggplot2:scale_fill_viridis_c]{ggplot2::scale_fill_viridis_c()}} for continuous fills. Ignored
when \code{log10 = FALSE} and the user wants the default.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Draw a ggplot2 heatmap of the \code{n_top} most abundant taxa (by total
read count) across the samples of a phyloseq object. The x-axis is
the sample, the y-axis is the taxon (with the chosen \code{taxa_rank}
name), and the fill is either the raw abundance (default) or the
log10-transformed abundance (\code{log10 = TRUE}). Useful for a quick
survey of the dominant taxa and to spot per-sample anomalies.
}
\examples{
\donttest{
data(data_fungi_mini, package = "MiscMetabar")
plot_taxa_heatmap_pq(data_fungi_mini, n_top = 15, taxa_rank = "Family")
plot_taxa_heatmap_pq(data_fungi_mini, n_top = 10, log10 = TRUE)
}
}
\author{
Adrien Taudière
}
