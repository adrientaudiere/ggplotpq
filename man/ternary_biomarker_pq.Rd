% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ternary_biomarker_pq.R
\name{ternary_biomarker_pq}
\alias{ternary_biomarker_pq}
\title{Ternary plot of biomarker taxa across three sample groups}
\usage{
ternary_biomarker_pq(
  physeq,
  fact,
  biomarker_taxa = NULL,
  level_order = NULL,
  raw = FALSE,
  point_alpha = 0.8,
  show_labels = FALSE,
  label_size = 2,
  size_range = c(1, 8)
)
}
\arguments{
\item{physeq}{(required) A \link[phyloseq:phyloseq-class]{phyloseq::phyloseq} object.}

\item{fact}{(required) Either a single character string matching a variable
name in \code{sample_data(physeq)}, or a factor of length \code{nsamples(physeq)}.
Must have exactly 3 levels.}

\item{biomarker_taxa}{(character vector, default \code{NULL}) Taxa names to plot
(e.g. biomarkers from a LefSe analysis). When \code{NULL}, all taxa are used.}

\item{level_order}{(character vector, default \code{NULL}) Order of the 3 levels
mapped to the (left = \code{x}, top = \code{y}, right = \code{z}) ternary axes. When
\code{NULL}, the factor's existing level order is used.}

\item{raw}{(logical, default \code{FALSE}) If \code{FALSE} (default), each group's
abundances are divided by the group total so that group size does not bias
the position. If \code{TRUE}, raw summed counts are used.}

\item{point_alpha}{(numeric, default \code{0.8}) Transparency of the points.}

\item{show_labels}{(logical, default \code{FALSE}) If \code{TRUE}, taxa names are added
next to each point with \code{\link[ggplot2:geom_text]{ggplot2::geom_text()}}.}

\item{label_size}{(numeric, default \code{2}) Font size for the taxa labels.}

\item{size_range}{(numeric of length 2, default \code{c(1, 8)}) Point size range
passed to \code{\link[ggplot2:scale_size]{ggplot2::scale_size()}}.}
}
\value{
A \code{ggtern}/\link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Renders biomarker (or any) taxa on a three-group ternary diagram using the
\pkg{ggtern} package. Each point is one taxon, positioned by its relative
abundance in the three groups defined by \code{fact}: a taxon near a corner is
enriched in that group. Point size encodes the taxon's total abundance and
point colour the group in which it is most abundant.

This is a \strong{pure rendering} helper: it does not run any biomarker-detection
analysis. The typical workflow is to detect biomarker taxa elsewhere (e.g. a
LefSe analysis, planned for the \code{netaipq} package) and pass the resulting
taxa names to \code{biomarker_taxa}. When \code{biomarker_taxa} is \code{NULL}, every taxon
is shown. The original published figure this reproduces colours ZOTUs by the
treatment (control / manure / frass) in which they are enriched (Wang et al.
2023, \doi{10.1007/s42832-023-0196-0}).
}
\examples{
\donttest{
data(data_fungi_mini, package = "MiscMetabar")
if (requireNamespace("ggtern", quietly = TRUE)) {
  # Height has 3 levels (High, Low, Middle); show all taxa
  ternary_biomarker_pq(data_fungi_mini, fact = "Height")
}
}
\dontrun{
# Restrict to biomarker taxa detected elsewhere (e.g. a LefSe analysis)
biomarkers <- c("ASV1", "ASV5", "ASV12")
ternary_biomarker_pq(
  data_fungi_mini,
  fact           = "Height",
  biomarker_taxa = biomarkers,
  show_labels    = TRUE
)
}
}
\references{
Wang et al. (2023) \doi{10.1007/s42832-023-0196-0}.
}
\seealso{
\code{\link[=ternary_pq]{ternary_pq()}} for a base-ggplot2 ternary/diamond of all taxa
(no \pkg{ggtern} dependency).
}
\author{
Adrien Taudière
}
