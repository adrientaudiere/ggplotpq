% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale_palette_pq.R
\name{scale_fill_tax_pq}
\alias{scale_fill_tax_pq}
\alias{scale_color_tax_pq}
\alias{scale_fill_sam_pq}
\alias{scale_color_sam_pq}
\title{Apply a stored palette to any ggplot}
\usage{
scale_fill_tax_pq(
  physeq,
  rank,
  suffix = "_color",
  identity = FALSE,
  drop = FALSE,
  limits = force,
  na.value = "grey70",
  ...
)

scale_color_tax_pq(
  physeq,
  rank,
  suffix = "_color",
  identity = FALSE,
  drop = FALSE,
  limits = force,
  na.value = "grey70",
  ...
)

scale_fill_sam_pq(
  physeq,
  var,
  suffix = "_color",
  identity = FALSE,
  drop = FALSE,
  limits = force,
  na.value = "grey70",
  ...
)

scale_color_sam_pq(
  physeq,
  var,
  suffix = "_color",
  identity = FALSE,
  drop = FALSE,
  limits = force,
  na.value = "grey70",
  ...
)
}
\arguments{
\item{physeq}{(phyloseq, required) An object carrying a colour column.}

\item{rank}{(character, required) A rank name from
\code{\link[phyloseq:rank_names]{phyloseq::rank_names()}}. For the \code{tax} scales.}

\item{suffix}{(character, default \code{"_color"}) Column-name suffix, as passed
to \code{\link[=palette_tax_pq]{palette_tax_pq()}} or \code{\link[=palette_sam_pq]{palette_sam_pq()}}.}

\item{identity}{(logical, default \code{FALSE}) The plot maps the colour column
itself rather than the level. See Details.}

\item{drop}{(logical, default \code{FALSE}) Keep unused factor levels in the
legend, so figures showing different subsets share one legend.}

\item{limits}{Passed to the underlying scale. The default \code{force} keeps
every level of the data, including those a subset does not use.}

\item{na.value}{(character, default \code{"grey70"}) Colour for levels absent
from the palette.}

\item{...}{Passed to the underlying \pkg{ggplot2} scale.}

\item{var}{(character, required) A variable name from
\code{\link[phyloseq:sample_variables]{phyloseq::sample_variables()}}. For the \code{sam} scales.}
}
\value{
A \pkg{ggplot2} scale, to be added to a plot with \code{+}.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Reads the palette a phyloseq object carries and turns it into a discrete
scale, whatever function produced the plot -- inside ggplotpq or outside it.
No plotting function needs to know that palettes exist.
}
\section{Which column is used}{
With the default \code{suffix}, the scale looks for \verb{<var>_color}. If that column
is missing but the object carries exactly one other \verb{<var>_color_*} column,
that one is used and a message says so; if several exist, the call fails and
lists them, because guessing would silently pick a palette. Passing \code{suffix}
explicitly always requires that exact column.
}

\section{\code{identity = TRUE}}{
Normally the plot maps the \emph{level} (\code{aes(fill = Order)}) and the scale
translates it into a colour. Some functions outside ggplotpq only let you
map an arbitrary column, in which case you map the colour column itself
(\code{aes(fill = Order_color)}) and the values are already hex strings. Set
\code{identity = TRUE} and the scale switches to
\code{\link[ggplot2:scale_fill_identity]{ggplot2::scale_fill_identity()}}, repairing the legend so it shows level
names rather than hex codes.
}

\section{No stored palette}{
These functions never build a palette on the fly. Recomputing one per figure
is exactly the instability the anchoring exists to prevent, so an object
without a colour column raises an error naming the two correct fixes: store
the palette with \code{add_to_phyloseq = TRUE}, or add it to the plot directly
with \code{p + palette_tax_pq(...)}.
}

\examples{
\donttest{
library(ggplot2)
data(data_fungi_mini, package = "MiscMetabar")

ps <- palette_tax_pq(
  data_fungi_mini, "Order",
  n = 8, add_to_phyloseq = TRUE
)

df <- as.data.frame(table(ps@tax_table@.Data[, "Order"]))
names(df) <- c("Order", "n")

ggplot(df, aes(Order, n, fill = Order)) +
  geom_col() +
  scale_fill_tax_pq(ps, "Order")
}

\dontrun{
# A plot built elsewhere that maps the colour column itself
df$Order_color <- ps@tax_table@.Data[
  match(df$Order, ps@tax_table@.Data[, "Order"]), "Order_color"
]
ggplot(df, aes(Order, n, fill = Order_color)) +
  geom_col() +
  scale_fill_tax_pq(ps, "Order", identity = TRUE)
}
}
\seealso{
\code{\link[=palette_tax_pq]{palette_tax_pq()}}, \code{\link[=palette_sam_pq]{palette_sam_pq()}}, \code{\link[=show_palette_pq]{show_palette_pq()}}.
}
\author{
Adrien Taudière
}
