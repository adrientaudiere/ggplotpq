% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/community_sharing_plot.R
\name{make_sharing_metric}
\alias{make_sharing_metric}
\title{Build a single metric definition for \code{community_sharing_plot()}}
\usage{
make_sharing_metric(
  label,
  color,
  fn,
  fmt = "\%.2f",
  prep = NULL,
  bounds = NULL
)
}
\arguments{
\item{label}{(character) Human-readable label used in the legend.}

\item{color}{(character) Colour of the curve connecting modalities.}

\item{fn}{(function) Function with signature
\verb{function(a, b, otu_sp, cache)} returning a single numeric value. \code{a}, \code{b}
are modality names; \code{otu_sp} is the species-level OTU table aggregated by
modality; \code{cache} is the object returned by \code{prep} (or \code{NULL}).}

\item{fmt}{(character, default \code{"\%.2f"}) \code{sprintf} format string for legend
stats (min / mean / max). Use \code{"\%.0f"} for integer counts.}

\item{prep}{(function, default \code{NULL}) Optional function
\verb{function(physeq, fact, modalities)} called once to precompute helper data
shared across all pairs (e.g. genus-level aggregation). Returns an object
passed as \code{cache} to \code{fn}.}

\item{bounds}{(numeric vector of length 2, default \code{NULL}) Theoretical range
\code{c(lower, upper)} of the metric. When non-\code{NULL}, linewidth is scaled to
\code{linewidth_range} using these fixed bounds, so the same metric value always
maps to the same linewidth regardless of the observed data. When \code{NULL},
linewidth is rescaled within the observed range of values. Use
\code{bounds = c(0, 1)} for proportions or similarities (e.g. Jaccard,
Bray-Curtis).}
}
\value{
A named list with the metric definition.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Helper constructor that packages one community-similarity metric for use by
\code{\link[=community_sharing_plot]{community_sharing_plot()}} and \code{\link[=community_sharing_barplot]{community_sharing_barplot()}}. All metrics
should be expressed as \strong{similarities} (higher value = more similar
communities = thicker link).
}
\examples{
\dontrun{
# Custom metric: number of unique (non-shared) species
unique_sp <- make_sharing_metric(
  label = "Unique species count",
  color = "#A65628",
  fmt   = "\%.0f",
  fn    = function(a, b, otu_sp, cache) {
    sum(xor(otu_sp[, a] > 0, otu_sp[, b] > 0))
  }
)

# Custom metric with prep step: Sorensen similarity at Family rank
sor_family <- make_sharing_metric(
  label = "Sorensen at Family rank",
  color = "#F781BF",
  prep  = function(physeq, fact, modalities) {
    d_fam <- phyloseq::tax_glom(physeq, "Family", NArm = FALSE)
    .agg_by_mod(d_fam, fact, modalities)
  },
  fn    = function(a, b, otu_sp, cache) {
    1 - as.numeric(vegan::vegdist(
      t(cache[, c(a, b)]),
      method = "bray", binary = TRUE
    ))
  }
)
}
}
\seealso{
\code{\link[=default_sharing_metrics]{default_sharing_metrics()}}, \code{\link[=community_sharing_plot]{community_sharing_plot()}}
}
\author{
Adrien Taudière
}
