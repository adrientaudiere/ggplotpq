% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palette_earthtones.R
\name{palette_earthtones}
\alias{palette_earthtones}
\title{Derive a colour palette from a geographic location}
\usage{
palette_earthtones(
  latitude,
  longitude,
  n_colors = 6,
  zoom = 10,
  bbox_size = NULL,
  provider = "Esri.WorldImagery",
  method = c("kmeans", "mean", "quantile"),
  sample_n = 10000,
  seed = 42
)
}
\arguments{
\item{latitude}{(numeric, required) Latitude of the target location
(decimal degrees, WGS84).}

\item{longitude}{(numeric, required) Longitude of the target location
(decimal degrees, WGS84).}

\item{n_colors}{(integer, default \code{6}) Number of colours to extract.}

\item{zoom}{(integer, default \code{10}) Tile zoom level passed to
\code{\link[maptiles:get_tiles]{maptiles::get_tiles()}}. Higher values show more detail but download
larger tiles.}

\item{bbox_size}{(numeric, default \code{NULL}) Half-side of the bounding square
in metres (in EPSG:3857). When \code{NULL}, it is set to
\code{5000 * (15 - zoom)} — the same heuristic used by \pkg{earthtones}.}

\item{provider}{(character, default \code{"Esri.WorldImagery"}) Tile provider
passed to \code{\link[maptiles:get_tiles]{maptiles::get_tiles()}}.}

\item{method}{(character, default \code{"kmeans"}) Clustering method: \code{"kmeans"}
(default) or \code{"mean"} / \code{"quantile"} for a simple central-tendency
single colour.}

\item{sample_n}{(integer, default \code{10000}) Maximum number of pixels to
sample before clustering. Reduces memory usage for high-zoom tiles.}

\item{seed}{(integer, default \code{42}) Random seed for k-means
reproducibility.}
}
\value{
A character vector of \code{n_colors} hex colour codes.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Retrieves a satellite or map tile centred on a geographic point (via
\pkg{maptiles}) and clusters the pixel colours with k-means to produce a
palette of dominant tones. Unlike the upstream \pkg{earthtones} package,
this implementation uses the bounding-box derived from the actual map
projection (EPSG:3857), which avoids the latitude-invariance bug present
in \pkg{earthtones} \eqn{\leq} 0.2.0.
}
\examples{
\dontrun{
# Palette from the Massif Central, France
cols <- palette_earthtones(latitude = 44.5, longitude = 3.2, n_colors = 5)
scales::show_col(cols)

# Palette from the Norwegian fjords (tests latitude sensitivity)
cols_north <- palette_earthtones(latitude = 60.4, longitude = 5.3, n_colors = 5)
scales::show_col(cols_north)
}
}
\seealso{
\code{\link[grDevices:rgb]{grDevices::rgb()}}, \code{\link[maptiles:get_tiles]{maptiles::get_tiles()}}, \code{\link[stats:kmeans]{stats::kmeans()}}
}
\author{
Adrien Taudière
}
